name: Integration

on:
  push:
    branches: [main]

jobs:
  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - run: echo "TODO: restore cached ngspice build or build from source"
      - run: ./gradlew test -Ptags=intg
